name: real_estate
channels:
  - conda-forge  # Сделали первым для лучшей совместимости ML пакетов
  - defaults
dependencies:
  # Основные используемые пакеты (упрощенные спецификации)
  - python=3.12
  - streamlit
  - pandas
  - numpy
  - altair
  - joblib=1.5.2 # Оставил версию, но убрал специфический хэш сборки
  - scikit-learn
  - scipy
  
  